# termsprawl — Completion Audit + Closeout Plan

**Date:** 2026-09-13 · **Repo:** termsprawl (app) @ `6d77815` (main, v0.25.0)
**Scope:** What is left to do to call the project **completed/finalized** — no new features.

---

## 1. Verified current state (2026-09-13, all re-derived — nothing from notes)

| Fact | Value | Evidence |
|---|---|---|
| App version | **0.25.0** (tag v0.25.0, 2026-09-06) | `package.json`, `git tag` |
| GitHub release | AppImage + .deb + `latest-linux.yml` present | `gh release view` read-back |
| Gitea release | v0.25.0 published, notes rendered; release run #380 **success** | Gitea API |
| Remote sync | main == origin == gitea == github (behind 0 / ahead 0 on all three) | `git rev-list --count` |
| Gates | **960/960 tests green (92 files, 12.5s); typecheck clean** | fresh run 2026-09-13 |
| Clean-room gate | OK (221 src files vs 546 prior; 1 known-benign block) | `check-originality.sh` |
| Tree cleanliness | `git status` clean, no open PRs, no `.github/workflows` | git/gh |
| termsprawl-web | `APP_VERSION = 0.25.0`, main synced gitea | `site.ts` |
| termsprawl-docs | main clean; docs current (relay pairing, cloud security, codex hooks) | git log |

**Prior claims contradicted:** memory/skill notes saying "Phase 9 partial, Phase 10 in progress, CI pending" and the 2026-09-04 plan's "chat permission cards UI missing" are all **stale** — see §2.

## 2. Every prior open item is closed (verified in the tree)

### 2026-08-29 full audit (docs/AUDIT-2026-08-29.md) — P0/P1/P2/P3 all DONE
- B1 Server Edition auth/scoping — shipped (WS token, loopback bind, shared validators, secret redaction).
- B3 chat tool-loop E2E — toolCalls wired, **permission cards render in ChatNode** (Approve/Deny, `ChatNode.tsx:369-395`), deny feedback works; FEATURES.md documents it.
- B4 cost chip — `conversationCost()` renders USD chip; `/cost` is a local note (`ChatNode.tsx:283`).
- B5 history cap — byte-capped serialization on persist (FEATURES.md line 57).
- B7 relay — disconnect on quit, preload/env surface, Settings → Connections Relay section, pairing + frames (v0.23.0).
- B9 `setWindowOpenHandler` deny — commit `7320965`.
- F1 light theme — full variable set (`--bg/--fg/--panel/--surface/…`); remaining `#0a0a0a` uses are intentional (lime-button text).
- F2 language selector — **removed** (`ce7f746`: "remove dead language field (English-only app)").
- F3 a11y — global `:focus-visible` lime ring, `prefers-reduced-motion` guard, `aria-live="polite"` transcript, `.chat-usage` at 0.62 alpha ≈ 4.6:1 (WCAG AA).
- F4 ChatNode UX — scroll-follow suppression, error retry, slash autocomplete all present.
- F5 z-index scale — `--z-canvas-chrome/overlay/toast/banner/popover` vars.
- F6 ChatNodeData dup — renderer copy deleted; single definition in `shared/types.ts:410`.
- F7 lazy-load — DiffNode/EditorNode lazy-imported in Canvas.tsx.
- F8 beforeunload flush — present in Canvas.tsx.
- I1 relay install step in CI + originality exception recorded — commit `7320965`.
- I2 relay `test` script — now `cd .. && pnpm vitest run relay/`.

### 2026-09-01 remaining-work checklist — all closed except one branch
A1 idle-stop sweeper ✓ · A2 7-day volume sweep ✓ · A3 SpaceCard live walk → **§4 user decision** · A4 poll cap ✓ · A5 changelog render ✓ (improvable) · B1 builder-first release ✓ · B2 deploy tars whole server dir ✓ · B3 space-e2e covers pty ✓ · B4 e2e gate documented in AGENTS.md ✓ (runner has no docker) · C1 hook honesty ✓ (codex normalizer + installer `9d804c4`; grok/agy honestly `hooks:false`) · C2 relay pairing + frames ✓ (v0.23.0) · C3 permission cards ✓ · C4 i18n field removed ✓ · C5 real storage bytes ✓ · C6 VPS upgrade deferred by trigger ✓ · D1 dompurify alerts 0 ✓ · D2 branch cleanup **PARTIAL** → §3.1 · D3 `server-new/` removed ✓

### 2026-09-04 remaining-features plan — all phases shipped
Phase 1 keyboard nav ✓ (v0.21.0) · Phase 2 edge labels ✓ (v0.21.0) · Phase 3 onboarding ✓ (v0.21.0) · Phase 4 codex hooks ✓ (v0.22.0) · Phase 5 chat permission cards ✓ (pre-0.20.0 — plan's "missing" claim was already stale) · Phase 6 relay pairing + frames ✓ (v0.23.0) · Phase 7 ops ✓ (documented gate)

### Multitenancy closeout (2026-09-06)
Phases 5+6 executed + deployed live (OAuth state binding, poll rate-limit, no-store, live-subscription entitlement, 30d sync-token TTL, 0600 store perms, two-account IDOR suite, JWT alg pinned HS256). Phase 3 host-ops → **§4 user decisions** (mostly documented/deferred).

---

## 3. Remaining gaps (completion work only — zero new features)

### 3.1 Docs/plan/branch hygiene (P2, ~1–2 h total, low risk)
1. **README.md:39** still says "Phases 0–12 of PLAN.md are shipped" — plan is now at Phase 19 + 6 deviations. Fix the sentence to point at PLAN.md as the living contract.
2. **PLAN.md Task 9.1** status line reads "PARTIAL" — superseded by 9.1b DONE (2026-08-27). Correct the line.
3. **PLAN.md Phase 17** closes with "merge + release pending user go-ahead" — github-import is fully in the tree and shipped. Update.
4. **PLAN.md Open Questions O-3/O-4** — effectively resolved (relay shipped + pairing; tmux ≥ 3.2 documented in AGENTS.md). Close or mark decided.
5. **PLAN.md 12.3** "PR #6 stays open until Actions enabled" contradicts AGENTS.md "GitHub Actions permanently dead" — workflow file and PR are gone; rewrite the caveat.
6. **Delete merged branch `security/harden-cdp-and-hooks`** (local + origin + gitea + github) — merged via `2138ce6`, still present on all three remotes.

### 3.2 Known CI noise (P2, optional)
- Gitea Actions **verify** jobs fail on every push (main and tag) while the release job succeeds — pre-existing since ~v0.22. If finalization means green CI, budget a session to find the verify failure (runner CT 109) or document it explicitly in AGENTS.md as accepted noise.

> **Update 2026-09-14 — CLOSED.** Not runner-specific: the verify job ran the
> suite before the renderer build existed, so `src/server/server-boot-gate.test.ts`
> 404'd the page-shell assertion on every fresh checkout. Fixed by
> `867540a` (build the renderer before the suite) plus `c89218a` (build Server
> Edition in the gate). Verify runs 250/251 (`284485f`) and 403 (`69c3093b`)
> are green — the job is no longer accepted noise; a red verify is a real
> failure again.

### 3.3 Accepted risks (documented; no action)
- B8 hook server loopback fail-open (by design), B10 relay client identity = invite code (cosmetic), B11 `termsprawl-file://` image-only scope (desktop; server shim denies), C6 VPS upgrade trigger-gated (RAM ≥ 60%).

---

## 4. User decisions needed (blockers to "finalized")

1. **SpaceCard 6-state live walk (A3)** — provision a Canvas-tier ($5/mo) test subscription, then walk none/provisioning/running/stopped/stopped-past-grace/deleted against production. Checklist already committed: `termsprawl-web/docs/SPACECARD-STATE-WALK.md`. *Only feature-surface item never eyeballed against the live API.*
2. **Multitenancy host-ops (Phase 3, hermes-box)** — **CLOSED: no changes applied**.
   The 2026-09-06 closeout already probe-verified ufw blocks all container→host
   listeners and documented that hermes + moltex keep their `0.0.0.0` binds
   (`docs/SPACES-OPS.md`). A molte loopback bind was attempted on 2026-09-13 and
   **REVERTED the same day** — Moltex is a separate project (out of scope); src +
   dist restored, pm2 restarted, listener verified back on `*:3005`. No host-ops
   changes remain planned; userns-remap stays deferred (documented rationale).
3. **v0.25.0 AppImage on the desktop** — **RESOLVED in v0.25.1 (2026-09-13).** The user's report
   ("some agents fail to spawn") had three distinct causes, all fixed and shipped:
   - **Antigravity**: the registry spawned `gemini`, which no longer exists on desktop installs
     (only `agy`). `command-resolver` now carries legacy aliases (`gemini → agy`/`antigravity`),
     so old persisted nodes work too.
   - **Claude**: `~/.local/bin/claude` was a dangling symlink (the npm package was missing from the
     box). Package reinstalled → 2.1.270 works.
   - **Codex**: termsprawl's codex hook installer wrote INVALID TOML (literal strings with
     shell-style doubled quotes) and the wrong table shape
     (`[[hooks.<Event>.hooks]]` without `[[hooks.<Event>]]`) — codex exited on every launch with a
     config parse error. Fixed to codex's schema with a boot-time self-repair of legacy blocks.
   - Bonus: missing-CLI nodes now print a clear notice instead of closing silently; the
     **OpenClaude preset** was added (local-model workflows) and its package reinstalled (0.30.0).
   - Shipped as **v0.25.1** (builder run 394 success, GitHub + Gitea assets verified, site + docs
     live at 0.25.1). Deploy scripts hardened: `chmod -R a+rX` after untar (a 077 build umask made
     the sites 404/403 — found and fixed live during this release).

## 5. Suggested closeout sequence

1. **P2 hygiene (one short session):** README + PLAN.md stale lines, delete `security/harden-cdp-and-hooks` on all remotes. No code, no release needed (or ride the next bump).
2. **Your decisions** on §4.1 and §4.2 — I can prep the exact commands/checklist for each once you pick.
3. **Optional final acceptance pass (I can run now):** `bash scripts/space-e2e.sh` + `bash scripts/github-import-e2e.sh` against current main, and a headless boot of the built 0.25.0 (gate-on/off CDP checks) — proves the shipped tag, not just main.
4. After that: the project is **finalized** — release cadence becomes fix-only, docs stay in sync per the cross-repo rule.
